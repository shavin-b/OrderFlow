<?xml version="1.0" encoding="utf-8"?>
<!--
    SPLASH SCREEN LAYOUT
    The window background is the WhatsApp gradient (set in Theme.OrderFlow.Splash).
    This layout adds the animated logo + text on top of that gradient background.

    Animation applied in SplashActivity:
    - Logo: scale from 0.8 → 1.0 + fade in (600ms, DecelerateInterpolator)
    - App name + tagline: fade in with 300ms delay

    Design intent:
    - Logo centered vertically (slightly above center with a bias of 0.42)
    - App name directly below logo
    - Tagline below the name in translucent white
    - Small pulsing progress indicator at the bottom
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/splash_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_splash_gradient">

    <!-- App Logo (white speech bubble icon on semi-transparent circle) -->
    <ImageView
        android:id="@+id/iv_logo"
        android:layout_width="@dimen/logo_size_splash"
        android:layout_height="@dimen/logo_size_splash"
        android:src="@drawable/ic_launcher_foreground"
        android:background="@color/wa_green"
        android:padding="10dp"
        android:contentDescription="OrderFlow logo"
        android:elevation="2dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tv_app_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:layout_constraintVertical_bias="0.42" />

    <!-- App Name -->
    <TextView
        android:id="@+id/tv_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="36sp"
        android:textStyle="bold"
        android:letterSpacing="0.04"
        app:layout_constraintTop_toBottomOf="@id/iv_logo"
        app:layout_constraintBottom_toTopOf="@id/tv_tagline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Tagline -->
    <TextView
        android:id="@+id/tv_tagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/app_tagline"
        android:textColor="#CCffffff"
        android:textSize="14sp"
        android:letterSpacing="0.06"
        app:layout_constraintTop_toBottomOf="@id/tv_app_name"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Loading dots at the very bottom -->
    <ProgressBar
        android:id="@+id/pb_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:indeterminate="true"
        android:indeterminateTint="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
