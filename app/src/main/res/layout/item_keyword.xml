<?xml version="1.0" encoding="utf-8"?>
<!--
    KEYWORD ITEM LAYOUT
    Represents a single keyword rule in the RecyclerView list.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.OrderFlow.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Active/Inactive Switch -->
        <com.google.android.material.materialswitch.MaterialSwitch
            android:id="@+id/switch_active"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="true"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Title (Comma separated keywords) -->
        <TextView
            android:id="@+id/tv_keywords"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:textColor="?attr/colorOnSurface"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="price, cost, rate"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/switch_active" />

        <!-- Meta info (Match Type & Priority) -->
        <TextView
            android:id="@+id/tv_meta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="13sp"
            tools:text="Match: PARTIAL  •  Priority: 10"
            app:layout_constraintTop_toBottomOf="@id/tv_keywords"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Linked Reply Label -->
        <TextView
            android:id="@+id/tv_linked_reply_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Linked Reply Template:"
            android:textColor="?attr/colorPrimary"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/tv_meta"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Reply ID (In future phases, this will resolve to the actual reply title) -->
        <TextView
            android:id="@+id/tv_reply_id"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:background="@drawable/bg_badge_success"
            android:backgroundTint="?attr/colorSurfaceVariant"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="13sp"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="reply_pricing_standard_v1"
            app:layout_constraintTop_toBottomOf="@id/tv_linked_reply_label"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
